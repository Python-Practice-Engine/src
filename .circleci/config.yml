version: 2.1
jobs:
  frontend:
    docker:
      - image: circleci/node:12.9.1-browser
    working_directory: ~/src/frontend
    steps:
      - checkout
      - restore_cache:
          keys:
          - v1-dependencies-{{ checksum "package.json" }}
          - v1-dependencies-

      - run: yarn install

      - save_cache:
          paths:
          - node_modules
          - ~/.npm
          - ~/.cache

          key: v1-dependencies-{{ checksum "package.json" }}

      - run: yarn-test

workflows:
  deploy:
    jobs:
      - frontend